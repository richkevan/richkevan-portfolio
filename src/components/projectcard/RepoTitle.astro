---
const { title, subTitle } = Astro.props;
---
    <div class="z-10 bg-neutral-base-700 bg-opacity-60">
      <div class="text-display-5 uppercase font-light">{title}</div>
      {subTitle && <div class="text-2xl font-semibold line-clamp-2 text-clip">{subTitle}</div>}
    </div>