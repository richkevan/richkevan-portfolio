---
const { live, github } = Astro.props;
---

    <div class="flex flex-row justify-around z-10 absolute left-0 bottom-5 w-full">
      <a href={live} target="_blank" class="rounded-full bg-primary-blue-700 px-4 font-semibold text-xl py-1">Live</a>
      <a href={github} target="_blank" class="rounded-full bg-primary-blue-700 px-4 font-semibold text-xl py-1">Github</a>
    </div>