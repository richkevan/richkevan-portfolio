---
const { tags } = Astro.props;
---

<div class="flex flex-wrap gap-1 h-min flex-row-reverse">
  {tags.map((tag: string,) => (
    <h4 class="font-medium text-center bg-neutral-blue-grey-600 h-min leading-8 rounded-lg whitespace-nowrap w-[10ch] px-2 overflow-clip">{tag}</h4>
  ))}
</div>
 