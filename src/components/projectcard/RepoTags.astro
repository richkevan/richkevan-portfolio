---
const { tags } = Astro.props;
---

<div class="flex flex-wrap gap-1 h-min flex-row-reverse">
  {tags.map((tag: string,) => (
    <h4 class="text-display-h6 font-medium text-center bg-neutral-blue-grey-600 w-28 h-min leading-8 rounded-lg whitespace-nowrap">{tag}</h4>
  ))}
</div>
 